class UMLSQueryEngine:
    def __init__(self, query_engines, cfg):
        self.query_engines = query_engines
        self.cfg = cfg

    def query(self, df):
        # Logic for running queries and collating results
        return df_curation
